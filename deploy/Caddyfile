{$SITE_ADDRESS:localhost} {
  encode gzip zstd

  @backend path /api/* /downloads/* /health /ready /docs /openapi.json
  handle @backend {
    reverse_proxy backend:8000
  }

  handle {
    reverse_proxy frontend:3000
  }
}
